#include"Node.h"

/*
template<typename T,typename T2>
class PriorityNode : public Node<T>
{
private:
	T2 priority;
public:
	PriorityNode(){next=nullptr;}
	PriorityNode(const T& r_Item, const T2& prio)
	{
		item = r_Item;
		next = nullptr;
		priority = prio;
	}
	Node(const T& r_Item,const T2& prio, Node<T>* nextNodePtr)
	{
		item = r_Item;
		next = nextNodePtr;
		priority = prio;
	}
	void setPrior(const T2& prio){priority=prio;}
	T2 getPrior(){return priority;}
};
*/


